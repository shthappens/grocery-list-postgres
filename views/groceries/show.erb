<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Groceries</title>
  </head>

  <body>
    <h3>Grocery: <%=@groceries_info[0]["name"] %></h3>

      <% @groceries_info.each do |grocery| %>
        <ul>
          Comment: <%=grocery["body"] %><br />
        </ul>
      <% end %>

      <fieldset>
        <legend>Add Comment</legend>
        <form method="post" action="/groceries/:id">
          <label for="comment">Comment</label>
          <input type="text" name="comment" id="body" />

          <input type="submit" value="Submit" />
        </form>
      </fieldset>

  </body>
</html>
